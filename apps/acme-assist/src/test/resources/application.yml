spring:
  autoconfigure:
    exclude:
      - org.springframework.ai.autoconfigure.vectorstore.pgvector.PgVectorStoreAutoConfiguration
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration #TODO this would be updated when we swap to TestContainer so we can have Postgres in Tests
  ai:
    openai:
      api-key: foobar-openai-key
      base-url: foobar-openai-baseurl
vectorstore: simple
